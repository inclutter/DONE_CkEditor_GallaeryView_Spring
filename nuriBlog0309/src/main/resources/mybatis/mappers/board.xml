<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
      PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
   "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
   
   <mapper namespace="mapper.board">
	<resultMap type="boardVO" id="boardResult">
		<result property="num" column="num" />
		<result property="subject" column="subject" />
		<result property="image_name" column="image_name" />
	</resultMap>

	<select id="selectBoard" resultMap="boardResult">
		<![CDATA[
			SELECT *
			FROM NURI_BOARD
			ORDER BY NUM DESC
		]]>
	</select>

	<insert id="insertBoard" parameterType="java.util.Map">
		<![CDATA[
		INSERT INTO NURI_BOARD(NUM, SUBJECT, CONTENT, WRITER, PASSWD, FILENAME)
		VALUES((SELECT NVL(MAX(NUM),0)+1 FROM NURI_BOARD),#{subject},#{content},#{writer},#{passwd, jdbcType=VARCHAR}, #{fileName, jdbcType=VARCHAR})
		]]>
	</insert>


</mapper> 